spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reborn_dev
    username: reborn
    password: reborn
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: INFO
    com.hyunjine.reborn: DEBUG
    org.springframework.http.server.reactive: DEBUG
    org.springframework.web.reactive: DEBUG
